[Time]
# Thor follows the H2 controller's NTP clock and serves it over the dedicated
# .125 link. Keep Ubuntu NTP as fallback when the robot link is unavailable.
NTP=192.168.125.163
FallbackNTP=ntp.ubuntu.com
PollIntervalMinSec=16
PollIntervalMaxSec=64
